"$schema" = "schemas/schema.json"

[default]
description = "Allows native Google authorization and cached PDF opening on Android."
permissions = ["allow-authorize", "allow-clearAccessToken", "allow-openPdf"]
