{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "http:default",
    "fs:default",
    "fs:allow-app-write",
    "dialog:default",
    "shell:allow-open"
  ]
}
