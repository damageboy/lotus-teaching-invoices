# src/components/RatesTab

| File                 | Purpose                                                                                                                            |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `index.tsx`          | Exports desktop `RatesTab`; shares `VersionBadge` with mobile settings. See change: cross-platform-dedup.                          |
| `MobileSettings.tsx` | Exports mobile settings and shared studio controls; shares `VersionBadge` with desktop settings. See change: cross-platform-dedup. |
